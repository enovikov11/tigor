# Beam Search into LLM Probability Space

> **Note:** This README was auto-generated by AI as a project summary. For detailed technical notes, see `README-tech.md`.

## What it does

Security research tool that performs beam search over LLM token probability distributions to find the N most likely outputs — used for security analysis, hallucination detection, and alignment research. Instead of random sampling, deterministically explores the most probable token sequences.

## Key Files
- `main.py` — Beam search solver with llama.cpp backend
- `project.json` — Project metadata
- `requirements.txt` — Python dependencies
- `http-api-examples.ipynb` — HTTP API examples for the solver

## Completion: 60%
## Coolness: 7/10
Novel approach to probing LLM probability space. Useful for security research (hallucination detection, adversarial inputs) and alignment. Paper and code are production-ready but could be expanded.
