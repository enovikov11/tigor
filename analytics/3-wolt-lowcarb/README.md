# Wolt Low-Carb Restaurant Finder
> **Note:** This README was auto-generated by AI as a project summary. For detailed technical notes, see `README-tech.md`.

Browser-based JavaScript snippet for extracting Wolt restaurant/menu data via their consumer API to find low-carb dining options in Belgrade.

## What it does
- Uses the Wolt consumer API (consumer-api.wolt.com) to fetch restaurant listings and venue information for Belgrade
- Extracts authentication token from browser cookies (`__wtoken`) for API authorization
- Fetches restaurants by coordinates and venue-list pages (Belgrade Milky area)
- Intended as a starting point for filtering menus to identify low-carb food options
- Runs in the browser console (uses `document.cookie` for auth)

## Key Files
- `index.js` — Browser console JavaScript: Wolt API auth extraction, restaurant fetching, venue list scraping (11 lines)
- `project.json` — Project metadata

## Completion: 10%
## Coolness: 4/10
Clever use of browser-side Wolt API extraction, but barely started — only has the data fetching skeleton. No actual menu parsing, low-carb filtering, or results processing implemented. The approach of using the in-browser Wolt auth token is pragmatic.
