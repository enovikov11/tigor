# Word Vector Analogy Explorer
> **Note:** This README was auto-generated by AI as a project summary. For detailed technical notes, see `README-tech.md`.

Research notebook exploring word embedding semantic relationships using GloVe vectors — finding surprising analogies and testing the geometric properties of word space.

## What it does
- Loads GloVe 50-dimensional word vectors (glove-wiki-gigaword-50) via gensim
- Reads custom word lists from local files and creates a submodel with only the matching words
- Tests classic word vector analogies: king - queen = man - woman, Hitler + Italy - Germany = Mussolini, sushi + Germany - Japan = bratwurst
- Implements `calc3(a, b, c)` for vector arithmetic: a + b - c, then finds nearest neighbors
- Runs a randomized search loop over a curated word list (`keep`), collecting the top-50 most coherent analogies by cosine similarity score
- Explores ideas for pluralization vectors, time-of-day arithmetic, and linear combinations of word vectors
- References 3Blue1Brown's LLM explanation video (14:20-17:50 timestamp)

## Key Files
- `main.ipynb` — Complete notebook: GloVe model loading, word list reading, analogy computation, random search with heap-based top-K tracking (124 lines)
- `project.json` — Project metadata (described as "Math research of embedding being different")

## Completion: 60%
## Coolness: 7/10
Fun exploration of word embedding spaces. The analogy discovery approach (random search over curated word lists, collecting top similarities) is a creative way to find surprising semantic relationships. The notebook has interesting research directions noted in comments (pluralization vectors, time arithmetic, linear combinations) that weren't fully explored. Classic NLP research in notebook form — educational and entertaining.
