# Simple Chess (C++)
> **Note:** This README was auto-generated by AI as a project summary. For detailed technical notes, see the project source.

## What it does
A C++ chess game prototype with planned network multiplayer support using Boost.Asio. The project sets up a CMake build system with Boost (system, filesystem, thread) and includes a static HTML frontend for the web-based game board. Currently a very early skeleton — the main source file contains only a partial TCP socket declaration.

## Key Files
- `CMakeLists.txt` - CMake build config: C++17, O3 optimization, Boost dependencies (system, filesystem, thread)
- `src/main.cpp` - Main entry point (partial — only includes Boost.Asio and an incomplete TCP address declaration)
- `static/index.html` - HTML frontend for the chess board UI

## Completion: 5%
## Coolness: 4/10
Ambitious C++ chess implementation with network play via Boost.Asio, but barely started — the source code is essentially a skeleton include statement. The CMake setup shows good structure, but the game logic and network protocol are unimplemented.
