# Scratchpad

ssh-keygen -R hostname
openssl rand -base64 12
lsof -iTCP -sTCP:LISTEN -P -n

python3Packages.heretic-llm

ssh root@192.168.1.31
/EFI/BOOT/BOOTX64.EFI
https://github.com/NixOS/nixpkgs/blob/master/nixos/modules/

scp ~/Desktop/monorepo/infra/0-box/ephermal/flake.nix box:/root/flake.nix
nixos-rebuild switch --flake .#host

openssl pciutils usbutils lsof iotop ethtool ipmitool smartmontools nvme-cli swtpm

canirun.ai

# Product

## Telegram search

## Изучить файлы

## Исследование следующих токенов в gpt

## heretic
