# infra/0-macbook — MacBook Setup Reference
> **Note:** This README was auto-generated by AI as a project summary. For detailed technical notes, see `README-tech.md`.

Personal MacBook configuration reference covering apps, tools, GPG/YubiKey, VS Code, and macOS settings.

## What it does

A single-reference document (`macbook.md`) documenting the personal MacBook setup:

- **Apps**: Telegram, Chrome, VS Code, Item2 (password manager), Bitwarden, WireGuard
- **Homebrew packages**: Python, WireGuard tools, rsync, libfido2, OpenSSH, miniserve
- **PGP/GPG + YubiKey**: Full workflow from `gpg --card-edit` to git signing (`commit.gpgsign true`), with `pinentry-mac` integration and key ID `4D98D893317CA780`
- **Git config**: user name "Evgenii Novikov", email `enovikov11@yandex.ru`, tgr.rs signing
- **macOS customizations**: screenshot location, Touch ID for sudo (pam_tid), VS Code settings (telemetry off, auto-save, light theme)
- **Python packages**: PyYAML, requests, ipykernel (pip3 with --break-system-packages)

## Key Files

- `macbook.md` — Complete setup reference (111 lines): brew installs, GPG/YubiKey setup, VS Code JSON config, git config, sudo Touch ID

## Completion: 100%
This is a reference doc, not software. It documents a completed setup comprehensively.

## Coolness: 3/10
Standard personal setup notes. Useful as a personal cookbook but not particularly notable. Good security hygiene (YubiKey + GPG signing) is the highlight.
