# infra/0-tgr — tgr.rs VPS Server (NixOS + Docker)
> **Note:** This README was auto-generated by AI as a project summary. For detailed technical notes, see `README-tech.md`.

NixOS configuration for the tgr.rs production VPS — Caddy reverse proxy, MariaDB, Telegram bots, and WireGuard VPN hub.

## What it does

This configures the **tgr.rs VPS** (167.86.90.24, static IP, NixOS on QEMU guest):

- **Caddy reverse proxy** (custom Dockerfile with mTLS support): serves `tgr.rs` (personal site with GitHub/LinkedIn redirects, 101bot widget) and `hardwave.tgr.rs`
- **MariaDB LTS** database for bot backends
- **Three Telegram bots** (pet101bot, honk101bot, beo101bot) built from `telegram/0-101bot-prod`, all connecting to shared MariaDB
- **WireGuard VPN gateway**: connects 4 peers (box, mac, iphone on 10.69.42.0/24, port 44222/udp)
- **Docker Compose** with security hardening: `cap_drop ALL`, `no-new-privileges:true`, non-root users, internal bridge networks
- **SSH key-only auth** (YubiKey FIDO2), firewall allowing ports 22/80/443/44222

Network isolation: `tgrdb` network (internal, for DB access), `honkbot` and `a101bot` networks for bot isolation.

## Key Files

- `configuration.nix` — NixOS config: networking, SSH, WireGuard, Docker, firewall (77 lines)
- `hardware-configuration.nix` — Auto-generated QEMU guest hardware (virtio, ext4)
- `docker-compose.yml` — Service stack: Caddy, MariaDB, 3 Telegram bots (99 lines)
- `caddy/Dockerfile` — Caddy image build (2 lines)
- `caddy/Caddyfile` — Caddy config: HTTPS redirect, tgr.rs, hardwave.tgr.rs, ACME (37 lines)
- `wg-genkeys.sh` — WireGuard key generation for server + 3 clients (box, mac, iphone)
- `ideas/readme.md` — Future plans: sha256-pinned podman images, GitHub CI, CAA records

## Completion: 90%
Production server with working services. Docker Compose stack is deployed and running. Ideas doc has minor improvements pending (podman migration, sha256 pinning).

## Coolness: 5/10
Solid production server config. Standard stack (Caddy + MariaDB + bots) with good security practices (non-root containers, internal networks, key-only SSH, WireGuard). Nothing exotic but well-executed.
