# RF CV Car Controllers

> **Note:** This README was auto-generated by AI as a project summary. For detailed technical notes, see the project source.

## What it does

Controller programs and ML tooling for a computer-controlled toy car. Part of the broader RF-CV car project (mirrored from github.com/rf-cv-car/rf-cv-car-controllers). Includes test scripts that send movement commands (forward, backward, diagonal) to a car controller via stdin.

## Key Files

- `test-controller-1/main.py` — Python script that cycles through movement commands (w/s/q/c for forward/back/left/right diagonals) at 500ms intervals
- `test-controller-1/run.sh` — Shell wrapper to launch the controller test
- `project.json` — Project metadata with GitHub sync config (stage: done, synced from external repo)

## Completion: 100%
## Coolness: 5/10
Physical robot car project with ML integration — the external repo (rf-cv-car) likely contains the vision/RF components that make this interesting.
