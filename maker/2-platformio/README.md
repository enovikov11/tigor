# PlatformIO ESP8266 Projects

> **Note:** This README was auto-generated by AI as a project summary. For detailed technical notes, see the project source.

## What it does

Collection of ESP8266 microcontroller projects built with PlatformIO. Includes a WiFi exponent calculator that scans for "vas100" prefixed SSIDs, computes SHA-256–based bit counts to derive an exponent, and broadcasts the result as a new access point name. Also contains incomplete "ip" and "vas100-lowlevel" projects using the ESP8266 non-OS SDK.

## Key Files

- `vas100-esp8266/src/main.cpp` — WiFi exponent calculator: scans networks, hashes "vas100" SSIDs with SHA-256, counts set bits in 100-bit window, broadcasts result as AP name
- `vas100-esp8266/src/sha256.c` / `sha256.h` — SHA-256 implementation for embedded use
- `vas100-esp8266/platformio.ini` — PlatformIO config for D1 Mini Pro (Arduino framework)
- `Projects/ip/` — Incomplete project stub with ESP8266 non-OS SDK
- `Projects/vas100-lowlevel/` — Low-level vas100 project using ESP8266 non-OS SDK
- `project.json` — Project metadata (stage: done, marked as dead)

## Completion: 100%
## Coolness: 5/10
Clever use of WiFi SSID scanning and on-device SHA-256 to create a distributed exponent computation system. The concept is creative even if the practical use case is unclear.
