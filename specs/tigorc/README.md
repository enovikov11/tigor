# specs/tigorc — Deterministic Markdown-to-Code Compiler with Local LLM
> **Note:** This README was auto-generated by AI as a project summary. For detailed technical notes, see `README-tech.md`.

Conceptual specification for a "compiler" that uses a local LLM to generate deterministic code from markdown specifications — where the README itself is both the spec and the runnable code.

## What it does

The README.md is a **dual-format file** that is valid both as Markdown and as a Python script. The concept has three stages:

1. **Stage 0 (Bootstrap)**: The markdown docstring is extracted and fed to a local LLM (Qwen2.5-1.5B-Instruct via llama.cpp, FP8 on GPU, seed=42, single-turn) via Docker. The LLM generates code from the markdown specification.
2. **Stage 1 (Source prompt)**: Defines how determinism is achieved (pinned model weights via SHA256, pinned seed) and states the architectural requirement: `tigorc.py` should equal `README.md` before `PROMPT_PAYLOAD_CUTOFF_POINT` + LLM output, and `Stage2(tigorc.py) == tigorc.py` (idempotent self-compilation).
3. **Stage 2 (Compiler code)**: Stub Python code (`print("123")`) where the actual compiler implementation would go.

### Key ideas
- **Intelligence as compression**: Store compressed specs instead of full implementations
- **Prompt as source**: Changing the LLM or tech stack doesn't require rewriting the spec
- **Self-compiling**: The README compiles itself (`bash README.md` runs it as Python)
- **Deterministic**: Pinned weights + pinned seed = reproducible output

## Key Files

- `README.md` — Self-contained spec + executable Python script (42 lines): markdown-to-code compiler concept with llama.cpp + Docker bootstrap

## Completion: 10%
The concept and bootstrap pipeline are defined (Stage 0 works conceptually). Stage 2 (actual compiler implementation) is a stub. The self-referential architecture (`tigorc.py == README.md + LLM`) is untested. This is a research spec, not a working system.

## Coolness: 8/10
The idea of a self-compiling markdown document that uses a local LLM to generate code from its own specification is genuinely novel and intellectually interesting. The "intelligence is compression" framing is provocative. Running the README itself as both spec and code (`bash README.md`) is clever. Not yet implemented, but the vision is compelling.
