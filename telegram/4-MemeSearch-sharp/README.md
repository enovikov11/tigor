# MemeSearch-sharp
> **Note:** This README was auto-generated by AI as a project summary. For detailed technical notes, see the project source.

## What it does
C# (.NET 8) Telegram bot stub that echoes user messages back. Originally planned to integrate Weaviate vector search + Microsoft Semantic Kernel for semantic meme search (the .NET counterpart to the Python MemeSearch), but the vector search code is commented out and non-functional.

## Key Files
- `Program.cs` - Entry point: reads env vars (MEMESEARCH_API_KEY, WEAVIATE_ENDPOINT, WEAVIATE_API_KEY), instantiates MemeBot. Vector store setup is commented out
- `MemeBot.cs` - Simple echo bot using Telegram.Bot library — subscribes to OnMessage and replies "you said: \<message\>"
- `MemeSearch.csproj` - .NET 8 project referencing Telegram.Bot, Microsoft.SemanticKernel, Weaviate connector
- `Dockerfile` - Multi-stage build: SDK image for build, ASP.NET runtime for execution
- `project.json` - Stage: draft, use: dead

## Completion: 15%
## Coolness: 3/10
Skeleton code that was never completed. The Dockerfile and project scaffolding are solid, but the core meme search functionality was never implemented. Useful as a template for future .NET Telegram bots.
