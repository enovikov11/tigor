# This-Manool-Doesnt-Exist
> **Note:** This README was auto-generated by AI as a project summary. For detailed technical notes, see the project source.

## What it does
Telegram inline bot that serves randomly selected "manool" (Pallas cat / манул) images. Uploads a dataset of manul photos to Yandex Cloud S3, indexes them in SQLite with a usage flag, and delivers images via inline query. Includes a dataset renaming utility for batch renaming image files.

## Key Files
- `bot/main.py` - Inline query handler: picks a random unused image from SQLite, marks it as used, returns as inline result
- `bot/image_uploader.py` - Uploads local images to Yandex Cloud S3 (manool bucket), registers URLs in SQLite, moves uploaded files to done/
- `bot/requirements.txt` - python-telegram-bot, sqlite3, boto3
- `bot/Dockerfile` - Python 3.8 container
- `rename_dataset.py` - Utility to rename dataset images to sequential format (dataset-0001.jpg, etc.)
- `project.json` - Stage: draft, use: dead

## Completion: 40%
## Coolness: 7/10
Cute concept — a Telegram bot for manul cat images. The S3 upload pipeline and SQLite tracking are functional, but the Dockerfile has a hardcoded placeholder token and the project is marked as dead. The manul (манул) is a beloved rare feline species in Russian internet culture.
