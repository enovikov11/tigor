# Redux IMG
> **Note:** This README was auto-generated by AI as a project summary. For detailed technical notes, see the project source.

## What it does
Image gallery viewer that displays a rendered image alongside its raw JSON data representation in an iframe. Contains two datasets: a music player UI mockup (MONATIK track) and a contest image collection. Demonstrates Redux state management for UI state.

## Key Files
- `index.html` - Split-view layout: image on left (60%), JSON iframe on right (40%)
- `music.json` - Redux state snapshot: user avatar, music player with track metadata, playlist with multiple tracks
- `music.png` - Screenshot/mockup of the music player UI
- `contest.json` - Redux state snapshot for contest image dataset
- `contest.png` - Screenshot/mockup of the contest UI
- `project.json` - Stage: draft, use: dead

## Completion: 20%
## Coolness: 4/10
Minimal proof-of-concept for Redux state visualization. The side-by-side image/JSON comparison is a clever debugging technique for UI state. Marked as dead — likely an early experiment in state management.
